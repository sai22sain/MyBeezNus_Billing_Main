# Empty = same-origin relative /api/... calls (backend runs as Vercel
# serverless functions on the same domain — see /vercel.json).
# Override with a full URL only if the backend is hosted elsewhere.
REACT_APP_API_URL=
